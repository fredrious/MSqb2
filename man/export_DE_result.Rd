% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_DE_result.R
\name{export_DE_result}
\alias{export_DE_result}
\title{Export Differential Expression Results to Excel}
\usage{
export_DE_result(
  dat,
  lfc.col = "logFC",
  apv.col = "adj.P.Val",
  gene.col = "Gene",
  prot.col = "Protein",
  negFill = "skyblue1",
  posFill = "pink",
  noSigFill = "grey90",
  sigFill = "red",
  sigFont = "darkred",
  tableStyle = "TableStyleLight8",
  file.name = "limma.stat_output",
  path = Tables.path,
  sig.level = 0.05,
  silent = FALSE
)
}
\arguments{
\item{dat}{A \code{data.table} containing the differential expression results. It should include columns for log-fold change, adjusted p-values, and identifiers for genes or proteins.}

\item{lfc.col}{A character string specifying the column name for the log-fold change values. Default is \code{"logFC"}.}

\item{apv.col}{A character string specifying the column name for the adjusted p-values. Default is \code{"adj.P.Val"}.}

\item{gene.col}{A character string specifying the column name for gene identifiers. Default is \code{"Gene"}.}

\item{prot.col}{A character string specifying the column name for protein identifiers. Default is \code{"Protein"}.}

\item{negFill}{A character string specifying the fill color for negative log-fold changes. Default is \code{"skyblue1"}.}

\item{posFill}{A character string specifying the fill color for positive log-fold changes. Default is \code{"pink"}.}

\item{noSigFill}{A character string specifying the fill color for non-significant results. Default is \code{"grey90"}.}

\item{sigFill}{A character string specifying the fill color for significant results. Default is \code{"red"}.}

\item{sigFont}{A character string specifying the font color for significant results. Default is \code{"darkred"}.}

\item{tableStyle}{A character string specifying the table style for the Excel sheets. Default is \code{"TableStyleLight8"}.}

\item{file.name}{A character string specifying the base name for the output Excel file. Default is \code{"limma.stat_output"}.}

\item{path}{A character string specifying the directory path where the Excel file will be saved. Default is \code{Tables.path}.}

\item{sig.level}{A numeric value specifying the significance level threshold for adjusted p-values. Default is \code{0.05}.}

\item{silent}{A logical value. If \code{TRUE}, suppresses messages during the function's execution. Default is \code{FALSE}.}
}
\value{
A \code{Workbook} object from the \code{openxlsx} package representing the Excel file that was created.
}
\description{
This function exports the results of a differential expression analysis to an Excel file. The results are organized into sheets by comparison, with significant results highlighted for easy identification.
}
\details{
The \code{export_DE_result} function processes the differential expression results, organizes them by comparison, and saves them into an Excel workbook. Each comparison is placed into a separate sheet. Significant results are highlighted with configurable colors and styles.

If comparison names exceed 30 characters, they are shortened, and a mapping file (\code{longCompNames.txt}) is generated to track the original and shortened names. This ensures compatibility with Excel's sheet name length limits.
}
\examples{
\dontrun{
  # Assuming `de_results` is a data.table with differential expression results
  export_DE_result(de_results, file.name = "DE_analysis_results")
}

}
