% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_psm_count.R
\name{add_psm_count}
\alias{add_psm_count}
\title{Add PSM Count and Related Information to msDT}
\usage{
add_psm_count(dat, param)
}
\arguments{
\item{dat}{A \code{data.table} containing the original MS data, typically with columns like \code{Feature}, \code{Protein}, \code{Abundance}, etc.}

\item{param}{A character string specifying the column name used for grouping when calculating PSM counts (e.g., \code{"Protein"}).}
}
\value{
A modified \code{data.table} with additional columns representing PSM count-related information, including mean PSM counts and abundance statistics.
}
\description{
This function calculates and adds PSM (Peptide-Spectrum Match) counts to the provided mass spectrometry data table (\code{msDT}).
}
\details{
The function performs the following steps:
\itemize{
\item Groups the data by the specified \code{param} and calculates the PSM count.
\item Computes the mean abundance for each group.
\item Adds a column representing the mean PSM count adjusted by the abundance rank.
\item Merges this information back into the original data table (\code{msDT}).
}
}
